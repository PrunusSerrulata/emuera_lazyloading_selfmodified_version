2026年4月22日
修复 SkiaSharp 文本渲染排版不对称问题：
	1. 修复 Config.cs：允许 SKIASHARP 模式也拥有全局偏移量 DrawingParam_ShapePositionShift，DrawableWidth 改为左右对称减去两倍偏移量，完美模拟 GDI+ 的 TextRenderer 边距行为。
	2. 修复 ConsoleDisplayLine.cs SetAlignment()：移除针对 SKIASHARP 的错误 padding hack，恢复纯净的对齐逻辑，让 LEFT/CENTER/RIGHT 坐标计算统一从 0 开始。
	3. 修复 ConsoleStyledString.cs DrawTo()：在渲染层统一加上 DrawingParam_ShapePositionShift，模拟 GDI+ 的 TextRenderer 边距。
	4. 修复 ConsoleStyledString.cs DrawToBitmap()：生成按钮位图时也加上 DrawingParam_ShapePositionShift，保持与 DrawTo 一致。
	修复效果：ALIGNMENT LEFT 文字不再贴边，与输入框（TextBox）左边距完全一致；CUSTOMDRAWLINE 分割线左右边距绝对对称；CENTER/RIGHT 对齐不再左右不对称；图像和形状与文字完美对齐。

重构图像资源管理 - 修复 SpriteAnime 动画渲染卡顿问题：
	1. 引入 SharedBitmapCache 全局共享位图池，解决 Sprite Sheet（多帧拼接大图）动画播放时同一文件被重复解码导致内存爆炸和性能下降的问题。
	2. 优化 ConstImage 架构，使其成为极轻量的共享外壳，不再独立管理 SKBitmap 实例，所有图像共享 SharedBitmapCache 的位图引用。
	3. 修复懒加载时 img.Width/img.Height 为 0 导致 fRect 变成 0,0,0,0 的 Bug：通过 SharedBitmapCache.GetInfo() 使用 SKCodec 只读文件头获取宽高，不解码像素。
	4. 优化 AppContents.LoadSpriteFromMeta()：同一动画内若多个帧指向同一文件，复用同一个 ConstImage 对象，避免重复创建。
	5. SharedBitmapCache.Clear() 在 SpriteDisposeAll() 时被调用，确保游戏重置时正确清空所有位图资源。

2026年4月18日
新增命令：SQL_CONNECT, SQL_DISCONNECT, SQL_EXECUTE_NONQUERY, SQL_EXECUTE_READER, SQL_READER_READ, SQL_READER_GET_LONG, SQL_READER_GET_STRING, SQL_READER_ISNULL, SQL_READER_CLOSE, SQL_EXECUTE_SCALAR_LONG, SQL_EXECUTE_SCALAR_STRING, SQL_IMPORT_MAP_XML, SQL_IMPORT_DT_XML, SQL_EXPORT_MAP_XML, SQL_EXPORT_DT_XML, SQL_IMPORT_XML_CUSTOM，SQL数据库操作函数集
见手册

重构 SqlManager：
	1. 引入流式 XML 解析（XmlReader/XmlWriter），支持 GB 级别的 MAP 和 DataTable 数据导入导出而无需占用大量内存。
	2. 优化 SQL 导入逻辑，自动建立 PRIMARY KEY 索引，提升后续懒加载查询性能。
	3. 实现了 SQL 与 XML 的深度交互，允许将 SQL 数据库作为大规模 XML 数据集的“后端索引层”。
	4. 新增 `SQL_IMPORT_XML_CUSTOM`，支持通过 XPath 映射将嵌套或不规则的 XML（如 MOD 扩展表）导入数据库，支持属性读取和 InnerXml 保留。

修复SqlManager.CloseAll()未集成到全局重置机制的Bug
	在GlobalStatic.Reset()中添加了SqlManager.CloseAll()调用，确保游戏重置时正确释放所有数据库连接和读取器资源

新增资源管理系统 (ResourceManager)：
	1. 引入二进制索引 (resources.idx) 加速（图像、动画）资源的元数据读取。
	2. 实现 LRU (Least Recently Used) 缓存淘汰机制，默认限制内存中同时存在的 Sprite 数量为 200，超过时自动释放最旧资源。
	3. 完善 G_ID 引用计数管理，支持多个 Sprite 安全共享同一个底层 Graphic 资源。
	4. 集成到 GlobalStatic.Reset()，确保游戏重置时彻底释放图像资源。
	5. 新增命令：RM_RESOURCECHECK_LOAD, RM_RELEASE_ALL, RM_RESOURCE_EXIST。见手册。

2026年4月16日
新增命令：EVAL/EVALS，动态求值，动态解析表达式函数语句
见手册

修复比较运算符GreaterEqualStrStr 和 LessEqualStrStr 类在字符串上的错误逻辑

2026年4月13日
新增命令：CALLSTR, JUMPSTR, TRYCALLSTR, TRYJUMPSTR, TRYCCALLSTR, TRYCJUMPSTR，动态执行命令式函数语句
见手册

修复 XML_ADDNODE 在匹配多个节点时只插入最后一次的 Bug
	在 .NET 的 XmlDocument 体系中，一个 XmlNode 实例只能拥有一个父节点。在旧代码中，当 `doSetAll` 为真且匹配到多个目标节点时，循环使用的是同一个 newChild 实例。这导致该节点在 DOM 树中不断被移动，最终只有最后一个目标节点成功添加了该子节点/属性。
	修复：
	引入了本地函数 `GetNewChild()`。
	- 对于节点 (Node) 操作，使用 `doc.ImportNode(sourceNodeForCopy, true)` 每次生成一个带有正确 OwnerDocument 的深拷贝克隆节点。
	- 对于属性 (Attribute) 操作，每次循环都调用 `doc.CreateAttribute` 创建全新的属性实例。
	确保在批量插入时，每个目标节点都能获得独立的子节点/属性对象。
修复遗漏的本地化条目


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
2025年12月29日
大家好啊，我是说的道理
今天来点大家想看的东西啊

蛇也是蒸蒸日上了，现在有了能在一定程度上修改emuera运行方式的能力，在此记录一下相关改动，为后续可能使用本版本的相关开发者及口上作者提供基本的帮助
以下信息为EMUERA1.824+v22+EMv18+EEv53与EMUERA1.824+v22+EMv18+EEv53+Lazyloadingv2.4（蛇版使用emuera版本）的对比，输入输出的英文字符代表数据类型，括号代表可选项

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
==================================
EXISTVAR:
==================================
原版：

输入：string
返回：int
注释：

如果存在与 varName 同名的变量 / 常量则返回对应的正整数，否则返回 0。

为整数类型时，返回值 setbit 1
为字符串类型时，返回值 setbit 2
为常量类型时，返回值 setbit 3
为二维数组时，返回值 setbit 4
为三维数组时，返回值 setbit 5

命令 / 行内函数两种写法均有效。
==================================
改造：

输入：string,(int)
返回：int
注释：

如果存在第二输入变量，则将string所输入的变量视为一个具体的存储单元，判断是否存在，若存在返回1，不存在返回0
如果不存在第二输入变量，则按原版执行

命令 / 行内函数两种写法均有效。

例：

ERB代码：
PRINTFORML {EXISTVAR("CFLAG:0:0", 1)}
PRINTFORML {EXISTVAR("CFLAG")}
PRINTFORML {EXISTVAR("CFLAG:0:0")}

终端输出结果：
1
1
0
==========================================================================================================
==================================
EXISTFUNCTION（bug修复）:
==================================
原版：

无法读取Lazyloading包含且在一次Emuera运行周期内未运行的函数

==================================
改造：

现已可读取Lazyloading包含且在一次Emuera运行周期内未运行的函数，但检测后函数将预加载进入内存，因此如果检测大量Lazyloading函数，可能会造成大量内存占用

==========================================================================================================
==================================
.als文件（bug修复）:
==================================
原版：

序号10后的字符串指针无法被读取

==================================
改造：

现已修复，可正常读取序号10后的字符串指针

==========================================================================================================
==================================
SPRITECREATE:
==================================
原版：

不支持8参数和10参数写法，与csv不同，因此直接用SPRITECREATE无法完成通过.csv可完成的完整图像控制

==================================
改造：

现已修复，可在无.csv的情况下进行完整图像控制

==========================================================================================================
==================================
SoundTouch（代码库引入）：
==================================
原作者：

https://github.com/naudio/varispeed-sample/tree/master/VarispeedDemo/SoundTouch

by markheath

原版：

不支持音频变速

==================================
改造：

现已引入SoundTouch库，用以支持音频变速

==========================================================================================================
==================================
Sound.Naudio.cs：
==================================

原版：

不支持音频长度读取、现在播放时间读取、跳转、暂停、继续播放、音频变速

==================================
改造：

现已支持以上所有功能

==========================================================================================================
==================================
GETSOUNDORBGMINFO(新增):
==================================

读取SOUND通道或BGM通道的音频播放状态

输入：int, int(可省略)
返回：int
注释：

第一参数读取SOUND通道，第二参数读取所需的音频信息
音频信息总共有五种：音频总长度、当前播放进度、正在播放中、音频音量、音频速度
若第二参数存在，则1返回音频总长度、2返回当前播放进度、3返回正在播放中、4返回音频音量、5返回音频速度
返回值位于result:0中
若第二参数不存在，则同时返回所有信息，result:0返回音频总长度、result:1返回当前播放进度、result:2返回正在播放中、result:3返回音频音量、result:4返回音频速度

命令 / 行内函数两种写法均有效。

================================================================================================================
==================================
ISPLAYINGSOUND(新增):
==================================

判断SOUND通道是否正在播放

输入：int（可省略）
返回：int
注释：

输入一个参数以检查某个通道是否有音频正在播放，若正在播放，则返回通道编号，否则返回-1
省略参数的情况下将遍历所有通道，直到检测到正在播放的通道时返回通道编号，或者遍历完所有通道后返回-1

命令 / 行内函数两种写法均有效。

==================================
SOUNDCONTROL(新增):
==================================

控制SOUND通道的音频播放情况

输入：  ① int, int
		② int, int, int, int（可省略）
返回：int
注释：

情况①：第一参数为通道编号，第二参数为控制类型
控制类型总共有两种种：0为暂停，1为继续播放，2为停止

情况②：第一参数为通道编号，第二参数为变速操控编号（仅支持3），第三参数为变速倍率，第四参数为变速类型
若第四参数存在，则变速时音调会随速度改变
若不存在，这变速时音调不改变

以上两种情况下，若通道编号不存在，返回-1，若控制类型不存在，返回-2，若控制成功，返回1

命令 / 行内函数两种写法均有效。

================================================================================================================
==================================
ISPLAYINGBGM(新增)：
==================================

判断BGM通道是否正在播放

输入：null
输出：int
注释：

BGM只存在一个通道，因此无需输入，若正在播放，则返回1，否则返回0

命令 / 行内函数两种写法均有效。

================================================================================================================
==================================
BGMCONTROL(新增)：
==================================

控制BGM通道的音频播放情况

输入：  ① int
		② int, int, int（可省略）
返回：int
注释：

BGM只存在一个通道，因此无需输入通道编号

情况①：参数为控制类型
控制类型总共有两种种：0为暂停，1为继续播放，2为停止

情况②：第一参数为变速操控编号（仅支持3），第二参数为变速倍率，第三参数为变速类型
若第三参数存在，则变速时音调会随速度改变
若不存在，这变速时音调不改变

以上两种情况下，若通道编号不存在，返回-1，若控制类型不存在，返回-2，若控制成功，返回1

命令 / 行内函数两种写法均有效。
================================================================================================================


以上内容为本次更新内容，如有遗漏，请及时补充。